# 推幣機（`pg-coinpush`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-coinpush.yaml`

## 1. 一句話

點機台落幣、推板來回把整床幣擠向前緣、中央得分口掉落計分的推幣機——物理刻意「不送分」，純娛樂計分非博弈，致敬推幣機玩法類型。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-coinpush` / `game` / `機台` |
| status | `listed` |
| 模式 | 單人沙盒機台：開台得 20 幣、無限 +20 續幣，無關卡 |
| 得分 | 每枚從中央得分口推出 **10** 分（`POINTS_PER_COIN`）；邊溝漏幣 0 分 |
| 物理 | 圓形剛體＋矩形推板；固定步長 1/120s、接觸求解 6 passes；μ=0.34 庫倉摩擦 |
| 畫布 | 直向 420×640 canvas；俯視（後＝推板、前＝前緣） |
| 素材 | 全部程式繪製（`sprites.js`）＋WebAudio 合成音效（無圖檔資產） |
| 交付形 | 純 HTML＋CSS＋ESM JS；無 build；`npx vitest run` 測試 |

## 3. 完整規則（現行實作）

### 3.1 局流程與經濟

- 「開台」重置為已鋪滿幣床的新局：分數／推落／漏幣歸零、代幣回 **20** 枚（`START_CREDITS`）。
- 點機台上方任一 X 投幣：耗 **1** 枚、從 `DROP_Y=52` 落下。盤面存活幣達上限 **84**（`MAX_COINS`）時拒投。
- 幣用盡且盤面靜止後進入 `empty`；可「+20 幣」無限續投或重開——**娛樂點數無真實成本**。
- 落點鉗制在場內 `FIELD_LEFT+20`–`FIELD_RIGHT−20`；瞄準位置隨 pointermove 即時更新。

### 3.2 幀內物理（實際常數）

| 參數 | 值 | 意義 |
| --- | --- | --- |
| 固定步長 | `FIXED_DT=1/120`s，單幀 ≤24 步 | 幀率無關 |
| 重力 | GRAVITY=980 | 僅作用於下落中的幣 |
| 推板 | 高 36、振幅 42、週期 3.2s 正弦 | y 以 `SHELF_TOP+12.6+21` 為中心振盪 |
| 摩擦 | μ=0.34×g | 平檯面：推力消失即減速停住，速度 <0.5 直接歸零 |
| 牆彈性 | WALL_REST=0.35（側牆） | 幣間恢復係數 COIN_REST=0.18 |

- **不送分原則**：幣只被推板或別的幣推動；推板最多把幣加速到自身表面速度、回程不拖帶；幣–幣等質量對稱（穿透各退一半、衝量均分動量守恆）。
- 接觸鏈以 Gauss-Seidel 由後往前排序求解 6 次（`SOLVER_PASSES`），滿床時推力才能傳到前緣；y 差超過直徑即剪枝。

### 3.3 幣床與前緣

- 開台鋪設六方緊密排列（hex packing）滿床：自 `SEED_BACK_Y=250` 到前緣逐排鋪、奇偶列交錯，鄰幣確實接觸使推力可傳遞。
- 前緣 `FRONT_EDGE=470`：幣心越過才翻落。中央 ±92px（`PAYOUT_HALF_W`）為**得分口**（+10 分、震屏 0.4s）；兩側為**邊溝**（漏幣不計分）——這是唯一的「莊家優勢」來源，全在幾何不在暗力。

### 3.4 邊界處理

- 下落幣落地高度取「推板底＋r＋2」與檯面頂兩者較大者，落地水平速度 ×0.4。
- 側牆鉗制反彈；後界 `SHELL_TOP+r` 反彈；半懸幣（重心未過緣）永久留著。
- 事件串每幣去重取尾 10 筆供音效用（drop/land/clink/push/score/lost/deny/empty）。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 開台 | 新局（20 幣＋滿床） |
| 點點擊／觸控機台上半 | 該 X 落幣（耗 1 枚） |
| 移動指標 | 更新落點預覽 |
| +20 幣 | 無限續幣 |
| 重來 ／ 音效開關 | 重置與設定 |

- HUD：總分／剩餘代幣；狀態列播報投幣、推出 +10、邊溝漏幣與代幣用盡。推板振盪、幣閃光與得分 shake 由 `sprites.js` 程序繪製。
- Mobile-first 直向；禁 `alert`／`confirm`／`prompt`。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| （無）KV／localStorage | — | 本 repo 目前完全未接 `/api/kv` 也未用 localStorage；分數與代幣皆存記憶體 |

- functions.js 為 stub（回 `{ok,name,path}` JSON），無自訂 API。
- 新增跨局統計（生涯總分、累積推出枚數）一律走 `/api/kv/pg-coinpush-*`，禁止裸 localStorage 當權威。

## 6. 美術／音效／署名

- 無外部圖像資產：機台、代幣（hue 38–56 金色變化）、推板皆程序繪製；音效 WebAudio 合成（落幣、落地、幣碰 clink、推板 push、得分、漏幣、拒絕、空倉），無需 ATTRIBUTION。
- 若未來加入素材：拷進 `assets/`、新增 `ATTRIBUTION.md`（CC0 也須署名）、同步 `sam-manifest.json` 的 files。

## 7. 測試（`npx vitest run`）

現有覆蓋（`game.test.js`，17 例）：平檯面無自由前進（檯中／極緣靜止幣不動、受推幣短距離內停住）；重心過緣才翻落（懸空不計分）；幣–幣接觸符合牛頓第三定律（靜止重疊分離不生速度、傳遞動量守恆、壓縮幣堆不自走）；推板至多傳遞自身速度且回程不拖帶；中央口得分／邊溝漏幣分流；滿盤 84 枚拒投不扣幣；session 級（seeded random）：40 枚投入回收率介於 20%–65%、投入＝檯面＋推出＋漏出守恆、投邊溝明顯吃虧、停投後收斂不再付分。

改動物理參數或幾何必補對應不變量測試；`app.js`／`sprites.js` DOM 不在測試範圍。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS（ESM）；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；訊息一律狀態列。
3. Mobile-first；主操作（點擊落幣）不可 hover-only。
4. **純娛樂非博弈**：代幣免費無限補充、分數無法兌換任何真實價值；型錄與 README 的「純娛樂計分、非賭博」聲明必須保留，「莊家優勢」僅能存在於幾何（得分口 vs 邊溝）。
5. 物理守恆契約：不得加入任何「清塞外力」「隱形拉力」等送分機制；改物理先補不變量測試。
6. 新增跨局統計以 `/api/kv/{key}` 為唯一權威；禁止裸 localStorage 當權威。
7. 不自行載入 `sdk.js`；宿主注入 `window.PG`。本作未用 `PG.libs`（vanilla canvas 即可）。
8. 改動可執行邏輯前先寫失敗測試（TDD）；檔案清單變動須同步 `sam-manifest.json`。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：強化「押注落點→觀察連鎖」的期待感，不改變公平物理的核心認同。

**高優先**

1. **連擊加成**：5 秒內連續推出多枚給 ×1.2/×1.5 短暫倍率（UI 倒數條）——獎勵「瞄準推板相位」這個現有最深層的技巧，目前一次推出只有固定 10 分。
2. **手機震動＋慢鏡頭**：得分口翻落瞬間 `navigator.vibrate(30)`＋該幣 0.2s 放大描邊；漏幣則灰階下沉。強化得失對比。
3. **生涯統計 KV**：累積推出／漏幣枚數、最高單局分存 `/api/kv/pg-coinpush-stats`——現在重載歸零，長期目標缺位。

**中優先**

4. **特殊幣**：低機率投出金幣（50 分）或炸彈幣（落地清開周圍一圈）——豐富決策但需新增不變量測試（守恆仍須成立）。
5. **幣床佈局輪替**：階梯狀、中央障礙柱、斜向缺口等 3–4 種 seed 佈局每局隨機，打破滿床定式的單調。
6. **推板相位提示**：推板前方畫微弱箭頭顯示當下運動方向——讓「何時落幣」可被學習而非瞎猜。

**低優先**

7. clink 音高隨衝量大小變化（events 已攜帶觸發，僅缺參數化）。
8. 機台調校面板：推板週期／振幅滑桿（sandbox 模式），兼作物理參數的手感測試工具。
